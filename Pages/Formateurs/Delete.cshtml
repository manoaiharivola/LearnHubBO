@page
@model LearnHubBO.Pages.Formateurs.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Formateur</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Formateur.NomFormateur)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Formateur.NomFormateur)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Formateur.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Formateur.Email)
        </dd>
    </dl>
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="text-danger">
            @TempData["ErrorMessage"]
        </div>
        <br />
    }
    <form method="post">
        <input type="hidden" asp-for="Formateur.IdFormateur" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
